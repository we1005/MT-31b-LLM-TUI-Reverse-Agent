# 资产索引（脚本 / 文档 / 产物 —— 其他 agent 一眼找到东西）

> 项目里积累的全部可复用资产。改动/复现前先来这查"有没有现成的"。

---

## 1. `scripts/`（测试 · 判分 · 基准）

| 脚本 | 用途 | 依赖 lemonade? |
|---|---|---|
| `test-guards.ts` | 止损守卫回归(33，含 signal↔count A/B toggle + 边派生 6 单测) | 否(mock 模型) |
| `test-guards-signal.ts` | signal-gated 守卫**纯函数决策**单测(16) | 否 |
| `test-redact.ts` | 脱敏防火墙题库(40：泄露门/还原/一致性/分级/兜底/fail-closed/容错) | 否 |
| `test-advisor-agent.ts` | 云端顾问 × agent 循环接线(15，含 fake 模型把 agent 逼到 stuck 的 seam) | 否 |
| `test-advisor-local.ts` | 云端顾问真实 E2E(可 `ADVISOR_BACKEND=volcengine` 打外部云) | 是 |
| `test-mcp.ts` | MCP server 模式 | 否 |
| `tui-render.test.tsx` | TUI 无头交互测试(注入按键/CJK 完整/审批两分支/白屏+重叠回归) | 否 |
| `tui-real-session.test.tsx` | 真 Agent+工具+lemonade 2 轮会话插桩 | 是 |
| `test-resume.sh` | 笔记续传 | 视情况 |
| `score-anchors.py` | **确定性锚点召回判分器**(吃 bank-multi / bank-crack 两式，产 `_anchors.json`) | 否 |
| `benchmark-models.sh` | 多云端模型基准(从 .env 读 ARK_API_KEY 跑火山引擎 N 模型) | 云端 |
| `gen-tool-help.sh` / `demo.sh` | 工具帮助生成 / demo | — |

一键离线全套：`bun run test`。

## 2. `pi/`（用 pi-agent 驱动本地模型 + 前置案卷 —— 可复现产物）

| 文件 | 用途 |
|---|---|
| `lemonade-provider.ts` | pi 自定义 provider 扩展(registerProvider 接 lemonade，thinkingFormat=qwen-chat-template) |
| `re-discipline.txt` | RE 纪律系统提示(--append-system-prompt)——**本地模型不超时的最关键设置** |
| `audit-sysprompt.txt` | apk 只读防御审计系统提示(--system-prompt 整体替换) |
| `run-pi.sh` | 串行驱动 pi + 抓指标 runner(解析 session.jsonl) |
| `batch-ab.sh` | 多 seed × 2 模式(count/signal) × 多题型 单变量 A/B 批处理(串行) |
| `bank-pi.json` | pi 基准题库(中/高难度，含 GT 锚点) |
| `GT-easynotes.md` | EasyNotes 破解 ground truth(人工核实，判分用) |
| `frontanalysis-workflow.js` | 强模型前置分析 workflow 脚本(定位混淆破解点→case-file) |
| `example-casefile-device.md` | 强模型对 Device_Info 产出的案卷样例(去混淆图+破解点+待核对清单) |
| `README.md` | pi/ 说明 |

## 3. `docs-resources/`（48+ 沉淀；对接必读的重点）

| 文档 | 内容 |
|---|---|
| `pi-agent接入Qwen3.6-计划.md` | pi 接入完整计划(9-agent workflow + 红队定稿) |
| `pi-agent接入Qwen3.6-实测对比.md` | pi × 本地模型 实测：驱动方式/表现/与 rev-agent 对比/上限/最佳设置/前置案卷突破 |
| **`pi-agent实验复现手册.md`** | **逐条可手动复现，含所有 verbatim 提示词(给强模型的 4 路搜索+案卷综合+pi handoff)** |
| `框架化-把逆向负担从模型移到框架.md` | 三提议深度分析+红队(铁律=框架只注入 context+收 budget) |
| `框架化-MVP-0-1-守卫软化-实测.md` | signal-gated 守卫 A/B 实测(难题错→对，普通题速度追回) |
| `混合后端-云端顾问-{实现方案,交付与验证}.md` | 脱敏云端顾问设计+真实外部云 live 验证 |
| `顺路发现缓存-旁路语义记忆-深度分析.md` | 待办想法的调研裁决(默认不做) |
| `上下文记忆系统-架构设计.md` | 台账/记忆系统 V2 设计+消融 |
| `TUI交互测试方法.md` | TUI 可测性调研(配合 onboarding/TUI-输出代理与测试.md) |

## 4. 跨会话记忆 `~/.claude/projects/-Volumes-zhitai-7100-reverse-agent/memory/`
`MEMORY.md` 是索引（26 条）。新会话首轮按其顺序读。关键：`lemonade_single_concurrency` / `swa_stable_prefix` / `compliance_redlines` / `pi_agent_benchmark` / `framework_over_model_verdict` / `findings_cache_verdict` / `git_workflow_pref` / `workflow_results_to_md`。

## 5. 环境/路径速查
- rev-agent 代码：`/Volumes/zhitai-7100/reverse-agent/rev-agent`
- 逆向产物：`../work/{mt-jadx,mtmod-jadx,np-jadx,modv6-jadx}`（CTF）、`../apk-moded/<name>-jadx/`（9 破解 App）
- pi 源码：`/Volumes/zhitai-7100/pi-0.80.6`（@earendil-works/pi-coding-agent v0.80.6，需 `npm ci --ignore-scripts && npm run build`）
- lemonade：`http://192.168.9.101:13305/api/v1`，模型 `Huihui-Qwen3.6-35B-A3B-abliterated-ggml`（**单并发！**）
- .env（含 ARK_API_KEY 供火山云）：`rev-agent/.env`，键名 `ark-api-key`（**只读入 env，绝不 echo/入 git**）
