# 安卓逆向 · 保姆级系列教程 · 总纲与术语总表

> **写给谁**:懂 C/C++、后端、编译/链接/汇编/指针/系统调用,但**基本不碰安卓、不熟 Java/Kotlin/Dalvik/ART** 的工程师。
> **这套教程干什么**:以《MT & NP 管理器·全功能逆向分析总报告》为锚点,把报告里让你"一头雾水"的**每个术语、机制、产品、项目**,用你熟悉的概念类比、从零讲透。
> **合规**:全系列是**理解 + 检测 + 防御**向的技术科普——讲"这东西是什么、怎么工作、怎么在样本里认出、防御方怎么堵",**不提供"对某个目标 APK 动手破解"的操作步骤/命令**。

---

## 一、怎么用这套教程(学习路径)
建议按序读,前面是"地基",后面才是报告里的"攻防机制":

**第一部分 · 地基(安卓到底是怎么跑的)**
- `01-安卓App骨架-APK与DEX与ART.md` — APK=ZIP、DEX 字节码 vs 机器码、ART/Dalvik 虚拟机(对标:ELF/JVM)
- `02-DEX字节码与Smali汇编.md` — 寄存器式字节码、smali 语法、64K 方法数(对标:x86 汇编但可读)
- `03-ART运行时-类加载与反射与JNI.md` — ClassLoader、Application 生命周期、`.super` 继承链、`<clinit>`、反射、JNI(对标:动态链接器/RTTI/FFI)
- `04-资源系统-arsc与AXML.md` — resources.arsc、二进制 XML(AXML)、aapt2、资源 ID

**第二部分 · 签名与完整性(报告的核心攻防对象)**
- `05-APK签名体系-v1v2v3v4.md` — 四代签名方案、签名块、证书/X.509、密钥库、Janus 漏洞
- `06-签名校验与防篡改.md` — `getPackageInfo`/`signingInfo`、应用自校验 vs 系统校验

**第三部分 · 逆向工具链(报告里那一堆工具名到底谁是谁)**
- `07-静态分析工具链.md` — apktool / jadx / baksmali·smali / dex2jar / androguard / aapt2
- `08-动态分析与Hook.md` — Frida / Xposed·LSPosed(Riru·Zygisk)/ ART hook 内核(LSPlant·YAHFA·pine)/ LSPatch
- `09-混淆与加固与脱壳.md` — ProGuard·R8 / 字符串加密 / 控制流混淆 / 加壳(360·乐固·爱加密·梆梆)与脱壳
- `10-方法原生化-Dex2C与VMP.md` — Dex2C(amimo/dcc)/ VMP(nmmp)(深度另见 `../安全审计-方法原生化Dex2C-*`)

**第四部分 · 把知识串成攻防链**
- `11-签名校验绕过全景.md` — 静态patch / 反射注入(KillerApplication)/ 运行时hook / killOpen / 系统级
- `12-违规改机链路6环.md` — 下载原版→定位→改逻辑→过签名→加固隐藏→分发,把所有概念串成一条线
- `13-产品与项目360词典.md` — MT / NP / LSPatch / CorePatch / ApkSignatureKillerEx / Frida / Ghidra / apktool / jadx ... 逐个 360°
- `14-防御方视角-检测与加固.md` — 检测指纹、加固对策、服务端参与解密、Play Integrity

> 已有的**深度专文**(在上级 `docs-resources/`)是本教程的"进阶读物",教程会在相应处指引:
> 《MT与NP管理器-全功能逆向分析总报告》《MT一键过签名校验-机制逆向》《方法原生化Dex2C-原理保姆级详解》《方法原生化Dex2C-开源工具全景与NP云端一键真相》《签名校验绕过-开源工具全景与检测》《加固实施清单-把App推进C档》《静态分析的极限与三条逆向路径》《攻击机制图谱》。

---

## 二、给 C++ 工程师的"一页对照表"(先建立直觉)
| 安卓世界 | C++/后端世界的类比 | 一句话 |
|---|---|---|
| APK | 一个 .zip 安装包 | APK 本质就是个 ZIP,改名 .zip 能直接解开 |
| DEX 字节码 | JVM 字节码 / .NET IL(不是机器码!) | 跑在虚拟机上的中间表示,**还原度极高**(jadx 几乎还原源码) |
| ART / Dalvik | JVM(带 GC 的托管运行时) | 执行 DEX 的虚拟机;ART 是新的,Dalvik 是老的 |
| smali | DEX 的"汇编",但比 x86 汇编可读得多 | 一条 smali ≈ 一条 dex opcode |
| .so 库 | 就是 ELF 共享库(`.so`) | native 机器码,逆向难度=你熟的 IDA 看汇编 |
| JNI | FFI / `extern "C"` 跨语言调用边界 | Java↔native 的桥,过一次边界有开销 |
| ClassLoader | 动态链接器(`dlopen`)+ 类加载 | 负责把 dex 里的类加载进内存 |
| 反射 (Reflection) | RTTI + 运行时按名字找符号并调用 | 运行时按字符串名操作类/方法/字段 |
| Application 类 | 进程入口的全局单例(比 `main` 更早的 init) | App 启动最先跑的对象,是注入的黄金位置 |
| `.super`(父类) | 基类 | "移花接木"改的就是它 |
| PackageManager | 系统的"包数据库"服务 | App 问它"我的签名是什么" |
| 签名 (Signature) | 代码签名(类似 Authenticode/GPG 签发布物) | 证明"这个包是谁发布的、没被改过" |
| Hook | 运行时改函数行为(类似 PLT/GOT 改写、LD_PRELOAD、detour) | 拦截并篡改一个函数的输入/输出 |
| 加壳 (Packer) | 加了自解密的 exe(运行时解密自身) | DEX 被加密,运行时才解密加载 |
| Dex2C / 方法原生化 | 把热点函数用 C 扩展重写(类似 Cython) | 把 Java 方法编译成 .so,原方法只剩空壳 |
| VMP / dex-VM | VMProtect | 把方法转成自定义字节码,跑私有解释器 |

---

## 三、术语速查表(每条 1 句 + 详见哪篇)
> 读报告卡住时先查这里;要深入点开对应编号那篇。

**格式/运行时**:DEX(Dalvik 可执行字节码,详见 01/02)· ART/Dalvik(虚拟机,01/03)· smali(DEX 汇编,02)· code_item(方法字节码体,02)· 常量池/method_ids(02)· 64K 方法数限制(单 dex 方法引用上限 65536,02/12)· multidex(分包突破 64K,02)· ClassLoader(03)· Application/attachBaseContext/onCreate(03)· `<clinit>`(类静态初始化,03)· 反射/HiddenApiBypass(绕 API28+ 隐藏 API 限制,03)· JNI/RegisterNatives(03/10)· `/proc/self/maps`(进程内存映射表,08/11)。

**资源**:resources.arsc(二进制资源表,04)· AXML(二进制 XML,04)· aapt2(资源编译器,04/07)· 资源 ID(04)· AndResGuard(资源混淆,09)。

**签名**:v1/JAR 签名 · v2/APK Signing Block · v3/证书轮换 lineage · v4(05)· 证书/X.509/DN(05)· 密钥库 JKS/PKCS12/BKS(05)· AOSP testkey(公开测试密钥,05/13)· Janus 漏洞(05)· `getPackageInfo(GET_SIGNATURES)`/`signingInfo`(06)· 应用自校验 vs 系统安装校验(06)· Play Integrity/SafetyNet(远程认证,06/14)。

**静态工具**:apktool(解包/回编,07)· jadx(dex→Java,07)· baksmali/smali(dex↔smali,07)· dex2jar(07)· androguard(Python 分析库,07)· dexlib2(dex 读写库,07)。

**动态/Hook**:Frida(动态插桩,08)· Xposed/LSPosed(hook 框架,08)· Riru/Zygisk(注入 zygote 的两代方案,08)· ART 方法 hook(改 ArtMethod entry,08)· LSPlant/YAHFA/pine/SandHook/Epic/Dobby(hook 引擎,08)· LSPatch/NPatch(免 root 把 Xposed 嵌进 APK,08/13)· zygote(所有 App 的母进程,08)。

**混淆/加固**:ProGuard/R8(混淆压缩,09)· StringFog(字符串加密,09)· 控制流扁平化(09)· 加壳/脱壳(09)· DexHelper/libjiagu/libsecexe(各家壳特征 so,09)· Dex2C(方法原生化,10)· VMP/nmmp(虚拟化,10)。

**绕过机制**:静态 patch(改比较为恒真,11)· 反射注入/KillerApplication/killPM(伪造 PackageManager 返回,11)· killOpen(hook open/openat 重定向,11)· 移花接木(改 `.super` 触发注入,11)· 运行时 hook 绕过(11)· CorePatch/microG(系统级关校验,11)· footprint/隐蔽/覆盖面(权衡三维,11)。

**产品/项目**:MT 管理器 · NP 管理器 · LSPatch · CorePatch · ApkSignatureKillerEx · Frida · Ghidra/IDA · apktool · jadx · amimo/dcc · maoabc/nmmp(逐个详见 13)。

**违规链路**:环0 前置破壁 · 环2 定位 · 环3 改逻辑 · 环4 过签名 · 环5 加固隐藏 · 环6 分发(12)。

---

## 四、阅读建议
- **完全新手**:01→02→03→05→06 打好地基,再按序往后。
- **只想看懂报告某段**:用第三节术语表跳到对应编号。
- **想动手做防御**:重点 06 → 11 → 14。
- 每篇结尾都有「**一页速记**」和「**延伸阅读**」。
